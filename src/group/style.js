import styled from "styled-components";

const style = StyleSheet.create({
    container: {
        background: '#1976d2',
        color: '#FFFFFF',
        fontSize: '20px',
        border: '2px solid black'
    }
})